"use client";

import type { ItemOut } from "@/lib/pm-types";

export function CutlistTab({ item: _ }: { item: ItemOut }) {
  return (
    <div className="rounded-lg border border-h-line bg-h-surface p-8 text-center text-h-muted">
      Cutlist editor — coming in T28.
    </div>
  );
}
